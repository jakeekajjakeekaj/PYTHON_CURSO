# Entradas de Datos (DATA INPUTS)

# nombre = input("Dame tu nombre: ");   # De esta manera el usuario puede ingresar datos
# print(nombre);
# print(f'El nombre es: {nombre}'); #Ejemplo de los fstrings
# ** SIEMPRE LOS INPUT DEVOLVERÁN TEXTO, AUNQUE SE ESCRIBA UN NÚMERO, EL VALOR SERÁ STRING EN VEZ DE INT O FLOAT
# Es decir si el usuario ingresa un var = 6, el valor será string, por lo que si después se indica el valor de var * 2, el resultado real debería ser 12, pero al ser un string, lo que se hará será 66

# numero = input("Ingresa un numero: ");  #Ejemplo de la manera correcta en que deben ser ingresados los números
# resultado = int(numero) * 2;    #Aquí se indica que el valor en vez de ser un string, es un int
# print(resultado);   #Ahora si se imprime el resultado de la multiplicación real.
# ** LO MISMO APLICA PARA LOS VALORES FLOAT **