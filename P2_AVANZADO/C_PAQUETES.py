# PAQUETES

#Para que a un paquete se le considere paquete, dentro de su carpeta, se debe tener el __init__.py

#Mientras que los módulos vendrían siendo por ejemplo un archivo python, los paquetes son muchos archivos python
#En pocas palabras es una carpeta con varios módulos

# suponiendo que tenemos una carpeta llamada paquete y queremos acceder a nuestro módulo saludar, que a su vez tiene la función saludar, esto sería:

# import paquete.saludar;

# print(paquete.saludar.saludar("Jake"));